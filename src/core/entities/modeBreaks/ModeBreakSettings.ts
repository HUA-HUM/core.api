export interface ModeBreakSettings {
  modeId: string;
  breakCount: number;
  breakDurationMinutes: number;
}

export interface SaveModeBreakSettingsData {
  modeId: string;
  breakCount: number;
  breakDurationMinutes: number;
}
